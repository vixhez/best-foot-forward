const RandomSortButton = ({ sortRandomly }) => (
    <button
        onClick={ sortRandomly }
        class="btn randomButton">
        Sort Players Randomly
    </button>
);

export default RandomSortButton;